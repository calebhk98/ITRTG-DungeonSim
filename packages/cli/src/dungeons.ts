/**
 * Dungeon registry access for the CLI.
 *
 * `getDungeon` and `DUNGEON_REGISTRY` are part of the public `@itrtg-sim/core`
 * barrel, so the CLI imports them directly.
 */
export { getDungeon, DUNGEON_REGISTRY } from '@itrtg-sim/core';
